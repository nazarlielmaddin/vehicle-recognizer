# vehicle-recognizer — production dependencies (Python 3.10+, pip format)
# GPU: CUDA 12.1+ for training (install torch CUDA build separately).

# --- core inference ---
torch>=2.2
torchvision>=0.17
timm>=0.9.16
ultralytics>=8.2
onnx>=1.16
onnxruntime>=1.18
numpy>=1.26
opencv-python>=4.9
# --- VLM review channel (SmolVLM-500M, Apache-2.0; v5 breaks it — pin v4) ---
transformers==4.49.0
# --- Qwen2.5-VL server expert (needs qwen-vl-utils only in local mode) ---
qwen-vl-utils>=0.0.6
Pillow>=10.3
scikit-learn>=1.4
scipy>=1.12

# --- zero-shot fallback (used only until trained classifiers exist) ---
open_clip_torch>=2.26

# --- embeddings / retrieval ---
faiss-cpu>=1.8  # optional; fallback to sklearn if unavailable
# faiss-gpu for CUDA builds (install manually)

# --- image quality / dedup ---
imagehash>=4.3

# --- explainability ---
grad-cam>=1.5  # optional, debug mode only (import pytorch_grad_cam)

# --- api / web ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2
aiofiles>=23.2

# --- training ---
albumentations>=1.4
pyyaml>=6.0
tqdm>=4.66
tensorboard>=2.16
pandas>=2.2
matplotlib>=3.8
seaborn>=0.13

# --- calibration / metrics ---
scikit-learn>=1.4  # ECE computed locally, no extra dep
netcal  # optional

# --- utils ---
typer>=0.12
rich>=13.7
python-dotenv>=1.0
httpx>=0.27
tqdm>=4.66

# --- tests ---
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
